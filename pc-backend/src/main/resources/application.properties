# Conexión a MariaDB
spring.datasource.url=jdbc:mariadb://localhost:3306/kimstore_db
spring.datasource.username=root
spring.datasource.password=KimJesus21
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# Configuración mágica de JPA (Hibernate)
# 'update' hace que Spring Boot cree y modifique las tablas automáticamente
# basándose en tus clases de Java
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.application.name=pc-backend
